<%= form_for(@scanner) do |form| %>
  <% if scanner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scanner.errors.count, "error") %> prohibited this scanner from being saved:</h2>

      <ul>
      <% scanner.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :scanner_name %>
    <%= form.text_field :scanner_name, id: :scanner_scanner_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
